# frontseat-pnpm-example

Node.js app using a pnpm lockfile — a real-world workspace used to validate Frontseat's `pnpm` plugin
(dependency provisioning from a committed lockfile, hermetic install on the grid).

```bash
frontseat build //
```
